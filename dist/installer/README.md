# Installer Drop Area

Inno Setup installer builds can be staged here after a local Windows build.

The PyInstaller onedir output under `dist/SolarExpanseFleetManager/` is ignored because it is build output. The final Inno Setup installer executable under this folder is intentionally allowed by `.gitignore` so versioned public installer drops can be committed or attached to GitHub Releases.

